import { HighlightDemo } from './highlight-demo';

describe('HighlightDemo', () => {
  it('should create an instance', () => {
    const directive = new HighlightDemo();
    expect(directive).toBeTruthy();
  });
});
